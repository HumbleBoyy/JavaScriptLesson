// Arrays

const fruits = ['apple', 'peaches', 'banana'];



fruits[2] = 'Olma';

// push() adds a new item to the array end
fruits.push("uzum");

// pop() removes last item an array
fruits.pop()

// unshift() adds a new item to the array begining
fruits.unshift('like')

console.log(fruits)